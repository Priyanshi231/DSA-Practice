class Solution {
public:
    int getLength(ListNode* head) {
        // Your code goes here
        int c = 1;

        ListNode* temp = head;

        while(temp->next != nullptr){
            temp = temp->next;
            c++;
        }
        return c;
    }
};
